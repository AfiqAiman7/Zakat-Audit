# Server Configuration
server.port=${PORT:9090}

# Database Configuration (PostgreSQL)
# Uses environment variables if available, otherwise defaults to local
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/audit_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Afiqaiman020130@}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# App Specific configurations
audit.system.timezone=Asia/Kuala_Lumpur
